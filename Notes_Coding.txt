			R

par(mfrow = c(x,x))
	numbers of graphs displayed

lm(y ~ x, data=...)
	least square linear regression 

pairs()
	subplots of all predictors

			
			
			Python

import matplotlib.pyplot as plt
	Plot package

plt.figure()
	Create plot

plt.plot(...)
	plot figure

new line in str: \nby
